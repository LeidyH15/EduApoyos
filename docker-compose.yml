services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: eduapoyos-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${SQL_SA_PASSWORD}"
    ports:
      - "1433:1433"
    volumes:
      - eduapoyos-sqlserver-data:/var/opt/mssql
    restart: unless-stopped

volumes:
  eduapoyos-sqlserver-data: